import { PacketId } from "./PacketId";

export abstract class AbstractPacket{

    // constructor(){
    //     PacketId.addpacketClassName2PacketId(this.constructor.name,this.getPacketId());
    //     PacketId.addPacketId2PacketClassName(this.getPacketId(),this.constructor.name);
    // }
    // public abstract getPacketId():number;
    
}